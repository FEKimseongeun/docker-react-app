sudo : required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t e97b318a5276 -f Dockerfile.dev .

script:
  - docker run -e CI=true e97b318a5276 npm run test --

after_success:
  - echo "Test Success"